# v3 작업 핸드오프 / TODO (기준일 2026-05-28)

기말 v3 파이프라인 본체(09→11→12→13) 완료 후 중단 시점 정리.

---

## ✅ 완료 (커밋됨)

| 노트북/분석 | 핵심 결과 |
|---|---|
| **nb09** 변수 재설계 | kospi(Granger p=0.57) 제외 → **예측셋 13변수** 확정. ablation: v3(0.614) > 과도축소(0.582). 전수 분포 C(13,5)=1285에서 대표5 상위 19%·비선택풀 100% 능가 |
| **nb11** 모델비교 | 진폭 RMSE≈naive(4.68 vs 4.86), **엣지는 1일 방향정확도 0.62**(XGB/LSTM, DM p≈0) |
| **nb12** 예측구간 | 고정 CQR 실패(fold3 99%/fold2 67%) → **온라인 롤링 conformal로 해결**(마진 0.905·폭 14.8·IS 20). 정직 결론: **제대로 보정하면 구간은 방향 못 줌**(0배제율≈0) |
| **nb13** 백테스트 | 1일 방향신호 1bp 비용후 **Sharpe 1.46, CI [0.56,2.40]**(0초과). 단 **수익 2022–23 집중**, 2021·2025 손실 |
| 분석 sweep | (1) 호라이즌 1·2·3·5·10일 → **다기간 가설 기각**(길수록 정확도↓). (2) 조합 전수 → 히스토그램 이상치=**fold2 6일 요행 아티팩트** |

**확정된 핵심 서사:** 진폭은 random walk·예측구간은 방향 불가 → **유일한 엣지 = 1일 방향(간밤 US→KR 스필오버)**, 추세장에서만 경제적 가치.

---

## ⏳ 남은 작업 (TODO)

1. **§3.2 2026 라이브 OOS (T3.1) — 최우선·진짜 검증대**
   - 1단계: **2026 신규 데이터 수집 가용성 확인** (현재 데이터 2025-12-31까지. 오늘 기준 ~5개월치 2026 존재할 것)
   - 2단계: 동결 모델 + 실제 신규 데이터 포워드 + 실제 수수료 → 라이브 dir acc·손익
2. **plan_md / 발표자료 정합성 동기화** (§아래 "해결할 문제" 1번과 연결)
3. **발표 흐름 통합 점검** — 09→11→12→13을 하나의 내러티브로 (그림 다수 reports/figures/v3/)
4. **(선택) nb10 SHAP dispersion** — F2(군집 SHAP·dendrogram), Tier 3. 시간 여력 시만
5. **(선택) kr_5Y/1Y VIF-복원 테스트** — feature-eng 재실행 필요(현재 kr_3y만 확장). 우선순위 낮음

---

## 🤔 결정 필요 (고민거리)

1. **차별점 재정의 확정** — 원래 T1 차별점이 "예측구간 축소+방향결합"이었으나 **그 가설은 기각됨**. 실제 차별점은 **① 1일 방향 엣지 ② 정직한 방법론**(변수선택·cherry-pick 함정·진폭/방향 분리·한계 규명). 발표 포지셔닝을 이쪽으로 재정렬할지 팀 합의 필요.
2. **CQR/예측구간 발표 비중** — "방향 무기"가 아니라 "한계 규명 + 온라인 conformal 방법론 기여"로 축소 제시할지. (강점은 정직성이지 성능 아님)
3. **T2(위기/레짐 전용 모델) 실제 구축 여부** — nb13의 "수익 2022–23 집중"이 T2를 강하게 동기화. 가장 큰 한계를 직접 해결하는 카드. nb14로 만들지/향후과제로 둘지.
4. **LSTM 위상** — v3에서 XGBoost와 거의 동률(약간 낮음). "비교 모델"로만 유지 확정.

---

## ⚠️ 해결해야 할 문제 / 리스크

1. **plan 문서와 실제 결과 불일치 (중요)** — plan §3.1은 *"비대칭·국소적응으로 좁히면 구간이 방향 신호를 담는다"* 고 적었으나 **실측에서 기각**(0배제율은 fold2 미달 부산물). plan 본문을 **실제 결론(온라인 conformal·방향은 q50)** 으로 정정해야 발표에서 자기모순 안 생김.
2. **백테스트 비용 민감성 (Q&A 공격 포인트)** — 회전율 92%·1bp 편도 가정. **2bp면 상당 상쇄**. → 비용 sweep(0.5/1/2/3bp Sharpe) 추가로 선제 방어 권장.
3. **백테스트 alpha의 레짐 집중** — Sharpe CI>0이나 수익이 2022–23에 몰림. 부트스트랩이 집중을 완전히 반영 못 할 수 있음 → 보수적 점검(연도 제외/fold별) 고려.
4. **데이터 종료 2025-12-31** — 모든 "2025" 평가는 종가 한정. 2026 라이브가 없으면 OOS 주장 약함(= TODO 1의 이유).
5. (해결됨, 기록용) CQR 코드 버그 아님(v2와 일치). 고정 cal n≈120 작음 → 온라인으로 우회.

---

## 산출물 위치
- 노트북: `notebooks/09,11,12,13_*.ipynb`
- 스크립트: `scripts/16f`(LSTM v3), `33`(CQR), `34`(조합전수), `35`(CQR개선), `36`(호라이즌)
- 그림: `reports/figures/v3/`
- 예측·지표 CSV: `reports/no_leak_v2/predictions_xgb_v3_*`, `cqr_v3_summary.csv`, `horizon_diracc_v3.csv`, `combo_enum_v3.csv`
